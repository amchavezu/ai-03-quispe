# Pending handwritten verification

Add the user's photograph as `prop3-endpoint.jpg`. It should verify the
upper-endpoint case `p₂ = 1` in Proposition 3 and show why the cumulative gap
saturates after the first period.
